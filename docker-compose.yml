version: "3.9"

# SQLite is stored in ./data/sign_ai.db — no separate DB container.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sign_ai_app
    volumes:
      - .:/app
    env_file:
      - .env
    tty: true
    stdin_open: true
